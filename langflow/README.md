# Historical Langflow artifact

`stocksage-analysis.json` is preserved as a historical flow export. It is not
connected to the application, evaluation tooling, environment configuration,
or release infrastructure.
